import {ViewStyle} from 'react-native';

interface IProps {
  styleContainer?: ViewStyle;
  styleUnderLine?: ViewStyle;
  styleTitle?: ViewStyle;
  width?: number;
  title: string;
}

export {IProps};
